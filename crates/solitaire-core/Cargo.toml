[package]
name = "solitaire-core"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true
description = "Deterministic, headless Klondike draw-1 solitaire engine. Seeded deal, legal-move predicate, win detection, state hash. Stub until master-plan Phase 4."
