//! Deterministic, headless Klondike draw-1 solitaire engine (the games pond's
//! first new game).
//!
//! **Stub** — registered as a workspace member in master-plan Phase 1 so the
//! `members` list is frozen before the parallel game phases. The real engine
//! (rules doc + tie-break tables + golden vectors, grown red-first, with a
//! `state_hash`) lands in master-plan Phase 4, mirroring the `match3-core`
//! discipline. No public API yet, by design.
