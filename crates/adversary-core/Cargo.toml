[package]
name = "adversary-core"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true
description = "Shared abstraction for two-player, perfect-information, turn-based, zero-sum games on the shelf (Drop 4, checkers, chess): the Adversary trait plus Side/MatchResult. Rules live in each game core; this crate is the reusable spine the harness and solvers are generic over. No network."

[dependencies]
serde = { workspace = true }
