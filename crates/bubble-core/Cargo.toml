[package]
name = "bubble-core"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true
description = "Deterministic, headless bubble-shooter engine (Puzzle Bobble family). Staggered hex board, seeded deal, connected-cluster pop + floating-cluster drop, state hash. Tap-target aim (no continuous physics), so replay is exact. No specials, no network."

[dependencies]
rand.workspace = true
rand_chacha.workspace = true
serde = { workspace = true }
serde_json.workspace = true
sha2.workspace = true
hex.workspace = true
thiserror.workspace = true
