[package]
name = "pond-outcome"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true
description = "P8 verifiable-outcome record: a self-checking result produced by replaying a move list against a game core's state hash. Game-agnostic (a Game trait); local-only."

[dependencies]
serde = { workspace = true }
serde_json.workspace = true
pond-docformat = { workspace = true }

[dev-dependencies]
solitaire-core = { workspace = true }
