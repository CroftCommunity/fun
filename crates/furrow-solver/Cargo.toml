[package]
name = "furrow-solver"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true
description = "Furrow (mancala) engine: an exact seed-margin solver below a measured seeds-in-play threshold, a depth-capped heuristic search above it, the shared class-preserving difficulty band, and an engine-grounded tutor whose claims are bound to a proven-exact flag."

[lints]
workspace = true

[dependencies]
adversary-core = { path = "../adversary-core" }
adversary-solver = { path = "../adversary-solver" }
furrow-core = { path = "../furrow-core" }
rand = { workspace = true }
rand_chacha = { workspace = true }
